/**
 * Expressly popup script.
 * Handles the popup and it's content related actions.
 **/
function xly_close_popup() {
    var popup = document.getElementById("xly-demo-popup");
    popup.remove(popup.selectedIndex);
}